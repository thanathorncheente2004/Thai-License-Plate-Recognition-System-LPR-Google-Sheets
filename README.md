# 🚗 Thai License Plate Recognition System (LPR) + Google Sheets 📊

ระบบตรวจจับและอ่านป้ายทะเบียนรถยนต์ไทยอัตโนมัติ (Thai LPR) พัฒนาด้วย **Python** และ **YOLOv8** มาพร้อมหน้าจอควบคุม (GUI) ที่ใช้งานง่าย สามารถบันทึกรูปภาพและส่งข้อมูลเลขทะเบียน วัน-เวลา สถานะการเข้า-ออก ขึ้น **Google Sheets** ได้ทันทีแบบ Real-time

![Demo Image](https://placehold.co/800x400?text=LPR+System+Dashboard) 
*(แนะนำ: เอารูปหน้าจอโปรแกรมตอนรันใส่แทนตรงนี้)*

---

## ✨ ฟีเจอร์หลัก (Key Features)

- **🔍 AI Detection:** ใช้โมเดล YOLOv8 ที่เทรนมาสำหรับป้ายทะเบียนไทยโดยเฉพาะ (อ่านภาษาไทยและตัวเลขได้แม่นยำ)
- **☁️ Google Sheets Sync:** บันทึกข้อมูล (ทะเบียน, วันที่, เวลา, สถานะ, ชื่อไฟล์รูป) ลง Google Sheets อัตโนมัติ
- **🖥️ Smart GUI:** หน้าจอควบคุมครบวงจร
  - สลับระหว่าง **Webcam** และ **Video File** ได้ทันที
  - ปรับขนาดและตำแหน่ง **Zone** ตรวจจับได้ง่ายๆ ด้วยการคลิก
  - รองรับ 2 โหมด: **จุดเดียว (Single)** และ **ทางเข้า-ออก (Entry/Exit)**
- **📂 Auto Organize:** ระบบจัดการไฟล์รูปแบบใหม่
  - บันทึกรูปป้ายทะเบียนลงโฟลเดอร์ `img/`
  - ตั้งชื่อไฟล์อัตโนมัติ: `ทะเบียน_วันที่_สถานะ.jpg`
- **📦 Portable:** มีสคริปต์สำหรับ Build เป็นไฟล์ `.exe` พร้อมใช้งาน (ไม่ต้องลง Python ที่เครื่องปลายทาง)

---

## 🛠️ ความต้องการระบบ (Prerequisites)

- Python 3.9 หรือใหม่กว่า
- Git (สำหรับดาวน์โหลดโค้ด)
- Google Account (สำหรับสร้าง API Key)

---

## ⚙️ การติดตั้งและตั้งค่า (Installation & Setup)

### ขั้นตอนที่ 1: ดาวน์โหลดโปรเจกต์
เปิด Terminal หรือ CMD แล้วพิมพ์คำสั่ง:
```bash
git clone [https://github.com/thanathorncheente2004/Thai-License-Plate-Recognition-LPR.git](https://github.com/thanathorncheente2004/Thai-License-Plate-Recognition-LPR.git)
cd Thai-License-Plate-Recognition-LPR
```

### ขั้นตอนที่ 2: ติดตั้ง Libraries ที่จำเป็น
เปิด Terminal หรือ CMD แล้วพิมพ์คำสั่ง:
```bash
pip install ultralytics opencv-python pillow gspread oauth2client pyinstaller pandas
```
(หมายเหตุ: หากใช้ GPU NVIDIA แนะนำให้ลง PyTorch เวอร์ชัน CUDA เพื่อความลื่นไหล)

### ขั้นตอนที่ 3: ตั้งค่า Google Sheets API (สำคัญมาก! 🔑)
ระบบนี้ต้องใช้ `credentials.json` เพื่อคุยกับ Google Sheet ให้ทำตามนี้:

- **1. สร้าง Project ใน Google Cloud** 
  - เข้าไปที่ [Google Cloud Console](https://console.cloud.google.com/?authuser=7)
  - สร้างโปรเจกต์ใหม่ (New Project) ตั้งชื่อว่า "Thai LPR"
- **2. เปิดใช้งาน API**
  - ไปที่เมนู **APIs & Services > Library**
  - ค้นหาและกด Enable ให้กับ 2 ตัวนี้:
    - 1. `Google Sheets API`
    - 2. `Google Drive API`
- **3. สร้างกุญแจ (Service Account)**
  - ไปที่เมนู **APIs & Services > Credentials**
  - กด **Create Credentials** > เลือก **Service Account**
  - ตั้งชื่อแล้วกด Next จนเสร็จ
  - คลิกที่ชื่อ Service Account ที่เพิ่งสร้าง > ไปที่แถบ **Keys**
  - กด **Add Key > Create new key** > เลือกแบบ **JSON**
  - ไฟล์จะถูกโหลดลงเครื่อง ให้เปลี่ยนชื่อไฟล์เป็น `credentials.json`
- **4. นำไฟล์ไปวาง**
  - นำไฟล์ `credentials.json` ไปใส่ในโฟลเดอร์ `json/` ของโปรเจกต์
- **5. แชร์สิทธิ์เข้าถึง Sheet**
  - เปิดไฟล์ `credentials.json` ด้วย Notepad มองหาบรรทัด `"client_email": "xxxx@xxxx.iam.gserviceaccount.com"`
  - ก๊อปปี้อีเมลนั้นไว้
  - ไปที่หน้า **Google Sheet** ของคุณ
  - กดปุ่มสีเขียว **Share (แชร์)** > วางอีเมลที่ก๊อปมา > เลือกสิทธิ์เป็น **Editor** > กด Send
  - **ระวัง:** ชื่อ Sheet (Tab ด้านล่างซ้าย) ต้องตรงกับในโค้ด (Default คือ `Sheet1` หรือแก้ใน `Main.py` บรรทัด `SHEET_NAME`)