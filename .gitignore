# --- ไฟล์ระบบและ Cache ---
__pycache__/
*.py[cod]
*$py.class

# --- ไฟล์ความลับ (ห้ามหลุดเด็ดขาด!) ---
json/credentials.json
credentials.json
config/credentials.json

# --- ไฟล์ที่ Build เป็น EXE แล้ว (ไม่ต้องเอาขึ้น มันหนัก) ---
build/
dist/
*.spec
*.exe

# --- ไฟล์มีเดียหนักๆ (ถ้าเน็ตแรงจะเอาออกก็ได้ แต่แนะนำให้ไม่เอาขึ้น) ---
test/*.mp4
*.avi
*.mov

# --- รูปที่แคปมาได้ (ไม่จำเป็นต้องเก็บ) ---
img/*.jpg
captured_plates/