# --- Security (สำคัญที่สุด ห้ามลบ) ---
# ป้องกัน Key ของ Google หลุดขึ้นเน็ต
json/credentials.json

# --- Python & System ---
# ไฟล์ขยะที่ Python สร้างขึ้นมาเอง
__pycache__/
*.pyc
.DS_Store

# --- Virtual Environment ---
# โฟลเดอร์จำลองสภาพแวดล้อม Python (ถ้ามี)
venv/
env/
.env

# --- Project Data ---
# ไม่เอาโฟลเดอร์รูปภาพที่เซฟไว้ (เพราะมันเยอะและรก)
img/

# ไม่เอาวิดีโอทดสอบ (เพราะไฟล์ใหญ่เกิน GitHub รับไหว)
test/
*.mp4
*.avi
*.mov

# --- IDE Settings ---
# การตั้งค่าของโปรแกรมเขียนโค้ด (VS Code / PyCharm)
.vscode/
.idea/